{
  "$schema": "https://opencode.ai/config.json",
  "model": "devstral-small-2",
  "instructions": [
    "opencode.md",
    "MANDATORY WORKFLOW: 1. sequential-thinking (PLAN) -> 2. web-search/context7 (GROUND) -> 3. shell (EXECUTE/VERIFY).",
    "REASONING: Use <thought> tags to decompose tasks before using any tool.",
    "GROUNDING: Use context7 for library syntax and web-search for errors. Assume nothing.",
    "VERIFICATION: Use shell for all file operations and tests (e.g., 'node --check', 'ls', 'cat').",
    "FORBIDDEN: Hallucinating file paths or API versions. If search fails, stop and report."
  ],
  "plugins": ["opencode-pty", "opencode-md-table-formatter"],
  "permission": {
    "edit": "ask",
    "bash": "ask"
  },
  "mcpServers": {
    "sequential-thinking": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-sequential-thinking"],
      "enabled": true
    },
    "context7": {
      "type": "local",
      "command": ["npx", "-y", "@upstash/context7-mcp"],
      "enabled": true
    },
    "web-search": {
      "type": "local",
      "command": ["npx", "-y", "web-search-mcp"],
      "enabled": true
    },
    "shell": {
      "type": "local",
      "command": ["npx", "-y", "@prodigy9/mcp-server-shell"],
      "enabled": true
    }
  }
}
